int editor(int argc, **u8 argv) {
	:editor__loop;
	int g = bread($$STDIN_FILENO$$);
	if (g < 0) {
		return 0 - g;
	}
	if (g == 0x03) {
		return 0;
	}
	bwrite($$STDOUT_FILENO$$, g as u8);
	goto editor__loop;
}
